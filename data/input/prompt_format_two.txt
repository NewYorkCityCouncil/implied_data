What I want you to return is described below. Please generate each section as follows: 

Section 1, Mission Summary:
Provide:
- a one or two sentence concise summary of the agency mission. 
- a list of the key programs they are required to run. 
- a one sentence concise explanation of the impacts the agency has on constituents.

Section 2, Data Summary: 
Next, provide a numbered list of the names of all datasets identified, ordered by their importance. Group datasets that are clearly and necessarily connected. 

Be thorough but concise, we need to make sure ALL important data is captured. Importance is defined by how critical the dataset is to the agency mission, and how important it is for oversight purposes.

Section 3, Data Details: 
Next, provide details for each of the datasets mentioned in part 1. For each dataset, include:
- A clear and specific title. Group datasets that are clearly and necessarily connected. 
- If there is a dataset from the list of datasets the agency has on Open Data that appears to match the dataset you're describing. Give the name of the open dataset (or multiple datasets), as well as a match percentage between how similar the description from the open data dataset is and your description is. Ensure the given matches are about the same **entity**, not just the same topic. 
- The most direct and relevant quotes that inform the dataset. If implied datasets are mentioned in multiple places, please highlight the best relevant quotes from each section. Each quote should be accompanied by the section number from which it was taken for later identifiability. Use direct quotes, though you can use ellipses to drop parts of paragraphs that aren't important. 
- If this dataset is matched by any of the open datasets the agency has posted. You can reply maybe if there's something related but not an exact match. Provide the name and link to the dataset if there is a match.   
- A single line, no longer than 30 words, describing why the text implies the data. 
- An explanation of what one row in the data represents (e.g. a single tree, an appointment, an application…)
- An explanation of what the data covers (e.g. for a dataset titled applications, specify what kinds of applications like "food vendor permits and special event permits")
- The important columns. Please highlight any columns that can be linked to another dataset. If any other dataset is referenced, you must include the number or numbers of the other datasets for clarity.

Section 4, Unmatched Open Datasets: 
Provide a list of Open Datasets that don't have a corollary in our list of datasets. If our list of datasets is complete, there should be no Open Datasets that can't be matched so this is an indicator of how well we've built the list of datasets. 

Group these where possible. If there are datasets for reports over multiple years combine them (eg. Instead of "Student Survey 2016" and "Student Survey 2017", list as "Student Surveys (2016, 2017)"). If there are datasets measuring similar things combein them (eg. Instead of "Demographics at NTA level" and "Demographics at Borough level", list as "Demographics (NTA, Borough)".  

Section 5, Non-tabular Data Summary: 
End with a summary of implied information that the agency may maintain, but not as tabular data. This might be paper applications, HR notes, and other information in that vein.
